
      <%= f.collection_check_boxes :category_ids, Category.all, :id, :name %>
    </div>
  </div>

  <div class="mb-3 row">
    <div class="col-sm-2 col-form-label">
      <%= f.label :category %>
    </div>

    <div class="col-sm-10">
      <%= f.fields_for :categories, @puzzle.categories.build do |categories_fields| %>
        <%= categories_fields.text_field :name, class: 'form-control' %>
    <div class="mb-3 row">   
      <% end %>
    </div>

    <div class="col-sm-2 col-form-label">
      <%= f.label :puzzle_categories %>
    </div>

    <div class="col-sm-10"> 
      <%= f.select :complexity, puzzle_complexities, {}, class: 'form-select form-select-lg' %>
    </div>

    


    
  